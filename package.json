{
  "name": "minicm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Clone pédagogique minimal de CodeMirror 6",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  }
}
